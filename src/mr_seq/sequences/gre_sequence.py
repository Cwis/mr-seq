def generate_gre(
    matrix_shape: tuple,
    title: str,
    outdir: str,
    physics_configuration_filename: str,
    timestep: int = -1,
    verbal: bool = False,
):
    if verbal:
        print(f"Generate GRE sequence:\n\tnamed: '{title}' in directory: '{outdir}'")

    return True
